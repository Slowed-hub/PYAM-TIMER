{
  "name": "discord-timer-bot",
  "version": "1.0.0",
  "description": "Bot Discord avec timer synchronisé pour le hangar PYAM",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Build complete - No build step required for Node.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "discord.js": "^14.22.1",
    "dotenv": "^17.2.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
